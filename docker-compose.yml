services:
  web:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/memarbashi:latest
    container_name: memarbashi
    restart: unless-stopped
    ports:
      - "3006:3000"   # host:container
    environment:
      - NODE_ENV=production
